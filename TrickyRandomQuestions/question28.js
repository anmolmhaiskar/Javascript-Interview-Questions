const x = (1, 2, 3, 4); // (expression1, expression2, ...) each value here is an expression and it returns the last expression
console.log(x); // 4

const y = (console.log(2), 2 * 2); // 2
console.log(y); // 4
